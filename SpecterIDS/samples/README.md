# Samples

`example.pcap` is generated from deterministic parser fixtures and contains a
single benign Ethernet/IPv4/TCP SYN packet for offline pipeline validation.

Regenerate it with:

```bash
make fixtures
```
